[package]
name = "catr"
version = "0.1.0"
description = "Rust cat"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }

[dev-dependencies]
assert_cmd = "1"
predicates = "2"
rand = "0.8"
